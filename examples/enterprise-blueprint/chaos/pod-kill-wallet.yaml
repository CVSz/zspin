apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-wallet
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  duration: "30s"
  selector:
    namespaces:
      - platform
    labelSelectors:
      app: wallet-service
